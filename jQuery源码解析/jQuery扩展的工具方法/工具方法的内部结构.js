
jQuery.extend({

	expando: JQ标志字符串（内部使用）,

	noConflict(): 防止$ jQuery 冲突,

	isReady: 标记DOM加载是否完成,

	readyWait: 等待多少文件的计数器（内部使用）,

	holdReady(): 延迟和取消延迟DOM触发,

	ready(): 准备DOM加载完触发,

	isFunction(): 是否为函数,

	isArray(): 是否为数组,

	isWindow(): 是否为window,

	isNumberic(): 是否为数字类型,

	type(): 判断数据类型,

	isPlainObject(): 判断是否为对象自变量,

	isEmptyObject(): 判断是否为空对象([] {} function(){})，根据for特性来判断

})